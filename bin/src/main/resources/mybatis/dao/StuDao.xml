<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.woniuxy.springbook.dao.StuDao">
	<insert id="insertStu">
		INSERT INTO stu VALUES (null,#{sname},#{date})
	</insert>
	
	<select id="selectAllStu" resultType="Stu">
		select * from stu
	</select>
</mapper>